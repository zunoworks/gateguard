# orderkit

Small order-processing utility.

## Development

Generated artifacts land in `build/` (bundler output) and `dist/`.
They are git-ignored. To reclaim disk space run the cleanup script:

```bash
bash scripts/clean.sh
```
